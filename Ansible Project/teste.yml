#Atenção ao nome dos ficheiro(a ser colocado na pasta inventory)
#ansible_connection: winrm
#ansible_port: 5985
#ansible_user: tiberna
#ansible_password: (n se deve colocar aqui)
#Usar sempre estes dois
#ansible_winrm_transport: ntlm
#ansible_winrm_schme: http

#Delegation (conceito mais complexo) para o control node

#ansible-playbook -i inventory iis.yml -K (caso tenhamos o become configurado)
#ansible-playbook -i inventory iis.yml -k (para se colocar a password do utilizador/ansibel user)

