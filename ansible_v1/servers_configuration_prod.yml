---
- name: Run all server configurations in production environment
  hosts: prod
  become: true
  roles:
    - php
    - apache
    - mysql_server
    - mysql_workbench
    - wordpress
    # Add more roles here as needed